Create a new React component: $ARGUMENTS

Rules:

1. File at client/src/components/$ComponentName.tsx
2. TypeScript: define Props interface at the top
3. Use Tailwind classes only — no inline styles
4. Use shadcn/ui primitives where appropriate
5. Export as named export: export const ComponentName = (...)
6. If it needs data, accept it as props — no direct API calls inside

Show me the complete component file.
